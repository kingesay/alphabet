{{>layout/header}}
<main class="contents">
<script>
    const beginning = new Date(2021, 2, 2, 12, 0, 0, 0);
    const end = new Date(2021, 5, 21, 12, 0, 0, 0);
    function getDayBetween(date1, date2) {
        const a = new Date(date1).getTime(), b = new Date(date2).getTime();
        return Math.round(Math.abs(a-b)/(1000*60*60*24));
	}

    function getProgress(){
        var time = getDayBetween(beginning, end);
        var now = new Date();
        var lastTime = getDayBetween(beginning, now);
        document.write('<progress value='+Math.floor((lastTime/time)*100)+' max='+100+'></progress>');
    }
</script>
    <div class="items">
        <div class="slide">
            <img class="mySlides" src="/img/img1.jpg" style="width: 100%; height: 100%">
            <img class="mySlides" src="/img/img2.jpg" style="width: 100%; height: 100%">
        </div>

        <div class="greeting">
            <p>안녕하세요 협성대학교 알고리즘 동아리 알파벳입니다.🙂</p>
            <p>언제나, 누구든, 어디에있든, 참여 할 수 있어요!</p>
        </div>

        <div class="item_text">
            <p class="hello_text">hello
            <p>
        </div>
        <div class="item_text">
            종강까지
            <script>
            getProgress();
            </script>
        </div>
    </div>
</main>
{{>layout/footer}}
