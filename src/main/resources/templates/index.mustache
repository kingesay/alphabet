{{>layout/header}}
<main class="contents">
    <div class="items">
        <div class="slide">
            <img class="mySlides" src="/img/img1.jpg" style="width: 100%; height: 100%">
            <img class="mySlides" src="/img/img2.jpg" style="width: 100%; height: 100%">
        </div>

        <div class="greeting">
            <p>안녕하세요 협성대학교 알고리즘 동아리 알파벳입니다.🙂</p>
            <p>언제나, 누구든, 어디에있든, 참여 할 수 있어요!</p>
        </div>

        <div class="item_text">
            <p class="hello_text">hello
            <p>
        </div>
        <div class="item_text">
            종강까지
            <progress id="progress" value="" max="100"></progress>
            <div id="progress_text"></div>
        </div>
    </div>
    {{#userName}}
        Logged in as <span id="user">{{userName}}</span>
        <a href="/logout" clas="btn btn-info active" role="button">Logout</a>
    {{/userName}}
    {{^userName}}
        <a href="/oauth2/authorization/google" class="btn btn-success active" role="button">Google Login</a>
    {{/userName}}
</main>
{{>layout/footer}}
